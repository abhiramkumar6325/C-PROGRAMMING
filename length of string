#include<stdio.h>
#include<string.h>

int main()
{
	char a[30];
	printf("enter the name:");
	fgets(a,sizeof(a),stdin);
	printf("name1:");
	puts(a);
	printf("the length is %d",strlen(a));
	return 0;
}
