#include<stdio.h>
#include<string.h>
int main()
{
	char a[100];
	int i;
    printf("enter the string ",a);
    scanf("%s",&a);
    printf("the characters of the string is ");
    for(i=0;i<strlen(a);i++)
    {
    	printf("%c ",a[i]);
	}
}
